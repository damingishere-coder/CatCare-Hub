# CatCare-Hub 开发进度

> 更新日期：2026-08-22

## 当前状态

- 当前完成轮次：P3｜订单系统
- 下一轮：P4｜订单计划与按天排程
- 正式开发依据：《猫咪喂养登记系统-开发执行文档.md》

## 轮次进度

| 轮次 | 内容 | 状态 |
| --- | --- | --- |
| P0 | 项目检查与基础骨架 | 已完成 |
| P1 | 数据库与核心业务模型 | 已完成 |
| P2 | 客户档案 | 已完成 |
| P3 | 订单系统 | 已完成 |
| P4 | 订单计划与按天排程 | 待开始 |
| P5 | 地图与路线 | 待开始 |
| P6 | 任务执行 | 待开始 |
| P7 | 工作台 | 待开始 |
| P8 | 收款记录 | 待开始 |
| P9 | 手机执行端 | 待开始 |
| P10 | 客户填写入口 | 待开始 |
| P11 | 响应式与 PWA | 待开始 |
| P12 | 安全与联动预留 | 待开始 |
| P13 | 测试与数据验证 | 待开始 |
| P14 | UI Design 重构 | 待开始，必须等待功能稳定 |

## P0 完成内容

- 记录空仓库、Git、本机运行环境和技术选型基线。
- 将正式开发执行文档纳入仓库根目录。
- 建立 React + TypeScript + Vite 前端骨架。
- 建立 FastAPI 后端骨架和 `/api/health` 健康检查。
- 建立 `/admin`、`/mobile`、`/fill`、`/fill/:token` 路由边界。
- 建立工作台、订单计划、客户档案、收款记录、设置五项后台导航。
- 建立覆盖环境变量、数据库、上传、日志、缓存、依赖和运行状态的 `.gitignore`。
- 提供 Windows `setup.bat`、`start.bat`、`stop.bat` 一键入口。
- 增加前端路由测试与后端健康检查测试。

## P0 验证结果

- `npm ci --prefix frontend`：通过，0 个已知漏洞。
- `npm run lint --prefix frontend`：通过。
- `npm run typecheck --prefix frontend`：通过。
- `npm test --prefix frontend -- --run`：通过，8 项测试全部成功。
- `npm run build --prefix frontend`：通过。
- `.\.venv\Scripts\python.exe -m pytest backend\tests`：通过，1 项测试成功。
- Windows 启动冒烟：API、后台、手机端和客户填写入口均返回 HTTP 200。
- Windows 停止冒烟：本项目的 8000 与 5180 监听均正确释放。
- Git 安全检查：未发现真实业务数据、环境变量、数据库、上传照片、日志或运行时文件进入暂存区。

## P1 完成内容

- 使用 SQLAlchemy 2 建立数据库 Base、引擎、会话和 SQLite 外键启用机制。
- 使用 Alembic 建立首个可升级、可降级迁移，迁移与模型自动差异检查无漂移。
- 建立 `customers`、`cats`、`orders`、`order_cats`、`tasks`、`task_items`、`task_photos`、`payments`、`customer_form_tokens`、`customer_form_submissions` 十张业务表。
- 为金额、日期范围、每日次数、坐标、任务顺序、状态、类型、唯一值和外键建立数据库约束。
- 为客户预留经纬度和地理编码状态，为猫咪预留停用状态，为图片只保存路径。
- 建立不含真实资料或有效 Token 的幂等虚构开发 Seed。
- 提供 `migrate.bat`、`seed.bat`，并让 `setup.bat` 自动迁移本地数据库。
- 补充 SQLite WAL/SHM 侧文件忽略规则。

## P1 验证结果

- Python 编译检查：通过。
- 后端测试：10 项全部通过，覆盖连接配置、迁移往返、模型漂移、十表关系、数据库约束、外键和 Seed 幂等性。
- 默认本地数据库：成功升级到 `0001_initial_schema (head)`。
- 虚构 Seed：首次写入成功，重复执行正确跳过。
- `migrate.bat`、`seed.bat`：在当前带空格的 Windows 路径下通过。
- P0 前端回归：lint、类型检查、8 项测试和生产构建全部通过。
- Windows 启停回归：API 和后台可访问，PowerShell 7 与 Windows PowerShell 均可安全识别并停止本项目进程，停止后 8000 与 5180 无残留监听。

## P2 完成内容

- 建立 `/api/admin/customers` 客户列表、搜索、新增、查看和编辑 API。
- 建立一个客户下猫咪新增、编辑、停用与恢复 API；停用不物理删除历史档案。
- 搜索覆盖客户姓名、微信昵称、手机号、小区和猫咪名字，并使用请求体避免搜索词进入访问日志 URL。
- 客户列表响应仅包含必要摘要，不批量返回详细地址、门禁、入户、钥匙编号或客户备注。
- 将 `/admin/customers` 从占位页升级为客户列表与详情主从页面。
- 客户表单覆盖联系方式、地址、门禁、钥匙、备注与老客户状态。
- 猫咪表单覆盖图片路径、性别、年龄、品种、性格、主食、偏好、猫砂、用药、特殊情况和服务注意事项。
- 增加加载、空结果、错误、保存和停用状态，并让 Vite `/api` 代理连通本地 FastAPI。

## P2 验证结果

- 后端测试：18 项全部通过，其中 8 项覆盖客户/多猫完整录入、编辑、搜索、URL 隐私、响应最小化、停用恢复、归属边界和输入校验。
- 前端测试：13 项全部通过，其中 5 项覆盖详情隐私边界、搜索请求体、新增客户、添加猫咪、停用和空结果。
- 前端 lint、TypeScript 类型检查与生产构建：全部通过。
- Alembic 自动差异检查：模型与迁移无漂移，本轮无需新增迁移。
- Windows 临时库联调：通过 Vite 代理新增 1 位虚构客户和 2 只虚构猫咪，按猫名搜索命中、停用状态持久化、详情返回 2 只猫咪。
- Windows 启停：API 与 `/admin/customers` 均返回 HTTP 200，停止后 8000 与 5180 无残留监听。
- Git 安全边界：联调数据库位于被忽略的 `.runtime/`，未触碰默认本地业务库。

## P3 完成内容

- 建立 `/api/admin/orders` 订单列表、新增、查看、完整编辑和状态更新 API。
- 建立只包含客户 ID、姓名、小区和有效猫咪的表单选项 API，避免批量返回电话、地址、门禁、钥匙或客户备注。
- 按包含首尾日期的服务天数和每日次数自动生成任务，并为每个任务生成所选服务项目。
- 由后端统一计算服务天数、总次数、多猫加价、爬楼费、其他费用、订单总额、已收和待收金额；客户端不能提交总额覆盖计算结果。
- 支持待确认、已确认、进行中、已完成和已取消状态，并同步更新尚未执行的任务状态。
- 订单结构变更会原子重建未执行任务；一旦任务开始、完成、异常，或已有执行时间、已完成项目、照片，即拒绝重建以保护历史。
- 已有收款记录的订单不能更换客户；完成订单前必须完成全部任务。
- 将 `/admin/plans` 从占位页升级为订单列表与详情主从页面，支持创建、编辑、状态更新、费用预览和按日期查看已生成任务。
- 增加 `/admin/orders` 兼容入口并重定向到 `/admin/plans`，保持既定五项后台导航不变。

## P3 验证结果

- 后端测试：26 项全部通过，其中 8 项覆盖费用计算、7 天 7 任务、每日多次任务、服务项目、原子创建、编辑重建、历史保护、归属和状态边界。
- 前端测试：18 项全部通过，其中 5 项覆盖订单 API 请求边界、订单创建、编辑、状态更新、重定向和费用展示。
- 前端 lint、TypeScript 类型检查与生产构建：全部通过。
- Alembic 自动差异检查：模型与迁移无漂移，本轮复用 P1 既有表结构，无需新增迁移。
- Windows 临时库联调：通过 Vite 代理创建虚构客户、两只虚构猫咪及 7 天订单，正确生成 7 个任务并计算 ¥245；编辑为 3 天、每天 2 次后正确重建为 6 个任务并计算 ¥210。
- Windows 状态联调：取消和恢复订单后，全部可变任务状态同步；API、`/admin/plans` 和 `/admin/orders` 均返回 HTTP 200，停止后 8000 与 5180 无残留监听。
- 隐私与运行数据边界：订单列表客户摘要仅含 ID、姓名和小区；临时数据库位于被忽略的 `.runtime/`，未触碰默认本地业务库。

## P4 计划边界

下一轮在既有“订单计划”入口实现按天计划、地图区域和右侧任务/客户信息布局，并增加任务日期、计划时间和顺序调整。P3 没有提前实现地图路线、自动路线优化、手机执行、图片上传、客户填写、PWA 或完整鉴权。
